import { scopedRaf } from './chunk-HQQTSFXD.js';
import { tooltipContext } from './chunk-RBTESKWW.js';
import { hasProvidedContext, useContext } from 'maverick.js';
import { defineElement, Component } from 'maverick.js/element';

var Tooltip = class extends Component {
  onAttach(el) {
    if (hasProvidedContext(tooltipContext)) {
      scopedRaf(() => {
        if (!el.isConnected)
          return;
        const tooltip = useContext(tooltipContext);
        tooltip.oc(el);
      });
    }
    this.setAttributes({
      position: this.$props.position
    });
  }
};
Tooltip.el = defineElement({
  tagName: "media-tooltip",
  props: { position: "top center" }
});

export { Tooltip };
